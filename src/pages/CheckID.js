import React from "react";

function CheckID({id}) {
    const idcheck= () => {
        //내일.... 즉.. 7월 14일날 할것이다. 
    }

    return(
        <button onClick={idcheck}>아이디 중복확인</button>
    )
}

export default CheckID;